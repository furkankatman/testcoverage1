name: .NET Tests + Coverage

on:
  push:
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Restore
      run: dotnet restore testcoverage1/testcoverage1.sln

    - name: Build
      run: dotnet build testcoverage1/testcoverage1.sln --no-restore

    - name: Run tests with coverage
      run: dotnet test testcoverage1/testcoverage1.sln --no-build --collect:"XPlat Code Coverage" --settings .runSettings


    - name: Install ReportGenerator
      run: dotnet tool install -g dotnet-reportgenerator-globaltool

    - name: Generate coverage report
      run: |
        reportgenerator \
          -reports:**/coverage.cobertura.xml \
          -targetdir:coveragereport \
          -reporttypes:Html

    - name: Upload coverage report
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report
        path: coveragereport
